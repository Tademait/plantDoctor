import { View} from 'react-native';

function Separator() {
  
  return (
    <View style={{height: 1, width: '100%', backgroundColor: "#ccc", margin: 15}}/>
  );
}

export default Separator;
